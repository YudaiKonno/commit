import java.util.*;


public class Main {
    public static void main(String[] args) {
        // 自分の得意な言語で
        // Let's チャレンジ！！

        Scanner sc = new Scanner(System.in);
        int num = sc.nextInt();
        
        for(int i =0;i < 10;i++){
            System.out.println(num);
            num = (num + 1)%10; //％は余剰分（余を求める）あまりをnumに代入し１０を超えた場合は０からのスタートにしている
        }
        System.out.println(); //省略型で最後の変数の結果を表示する
    }
}
